agents/Engineering/.claude/settings.local.json
agents/WebScraper/.claude/settings.local.json
.claude/
release-manifest.json
.opencode/
.env
.env.*
!**/.env.example
!**/.env.tpl
**/.tmp/

# Dependencies — never track (was missing; caused thousands of untracked files)
node_modules/

# Compiled Python artifacts
__pycache__/

# Deploy bundles — regenerate with the project's build-dist.mjs
projects/stagecraft-studio-site/dist/

# Scratch — WebDesign screenshot/compare output, regenerated on demand
temporary-screenshots/

# Kept local (your "keep local" choice) — not published. Move to a private repo if you want them versioned.
/docs/TECH_STACK_DISCOVERY.md
/plans/260517-1834-cv-cover-letter-redesign/
/plans/260527-1234-mookie-pitch/
/plans/reports/research-260602-1138-pet-centre-market.md
/projects/pet-centre-marketing/
# Kept local — has 100MB+ video assets (exceed GitHub's file limit); host media on a
# CDN + commit only code if this ever needs versioning.
/projects/re-dei-sapori/
